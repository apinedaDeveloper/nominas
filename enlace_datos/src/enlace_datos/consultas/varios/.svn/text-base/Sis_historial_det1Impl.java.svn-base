package enlace_datos.consultas.varios;

import enlace_datos.consultas.varios.common.Sis_historial_det1;

import oracle.jbo.Row;
import oracle.jbo.RowIterator;
import oracle.jbo.domain.Number;
import oracle.jbo.server.ViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class Sis_historial_det1Impl extends ViewObjectImpl implements Sis_historial_det1 {

    private Number vTotalNominal;
    private Number vTotalBono;
    private Number vTotalDescuento;
    private Number vTotalLiquido;

    /**This is the default constructor (do not remove)
     */
    public Sis_historial_det1Impl() {
       
    }

    /**Gets the bind variable value for IdDoc
     */
    public Number getIdDoc() {
        return (Number)getNamedWhereClauseParam("IdDoc");
    }

    /**Sets <code>value</code> for bind variable IdDoc
     */
    public void setIdDoc(Number value) {
        setNamedWhereClauseParam("IdDoc", value);
    }
    
    public void obtenerTotales(){
        Sis_historial_det1RowImpl vFila;
        Row [] vFilas;
        vFilas=this.getAllRowsInRange();
        this.vTotalNominal=new Number(0);   
        this.vTotalBono=new Number(0);   
        this.vTotalDescuento=new Number(0);   
        this.vTotalLiquido=new Number(0);   
        if (vFilas!=null && vFilas.length>0){
            for (int i=0; i<vFilas.length;i++){
               vFila=(Sis_historial_det1RowImpl)vFilas[i];
               this.vTotalDescuento=this.vTotalDescuento.add(vFila.getTotalDescuento());
               this.vTotalBono=this.vTotalBono.add(vFila.getTotalBono());
               this.vTotalNominal=this.vTotalNominal.add(vFila.getNominal());
               this.vTotalLiquido=this.vTotalLiquido.add(vFila.getLiquido());
            }                               
            
        }
 }

public Number getTotalNominal(){
            obtenerTotales();
  return this.vTotalNominal;
   }
  
public Number getTotalBono(){
  if (this.vTotalBono==null){
           obtenerTotales();
     }
return this.vTotalBono;
       }
    
public Number getTotalDescuento(){
  if (this.vTotalDescuento==null){
         obtenerTotales();
     }
return this.vTotalDescuento;
  }

public Number getTotalLiquido(){
  if (this.vTotalLiquido==null){
          obtenerTotales();
   }
return this.vTotalLiquido;
}

}
