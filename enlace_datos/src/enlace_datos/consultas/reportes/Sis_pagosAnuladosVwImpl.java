package enlace_datos.consultas.reportes;


import enlace_datos.consultas.reportes.common.Sis_pagosAnuladosVw;

import enlace_datos.util.utils;

import java.sql.ResultSet;

import oracle.jbo.domain.Date;
import oracle.jbo.domain.Number;
import oracle.jbo.server.ViewObjectImpl;
import oracle.jbo.server.ViewRowImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class Sis_pagosAnuladosVwImpl extends ViewObjectImpl implements Sis_pagosAnuladosVw {
    /**This is the default constructor (do not remove)
     */
    public Sis_pagosAnuladosVwImpl() {
    }

    /**Gets the bind variable value for param
     */
    public String getparam() {
        return (String)getNamedWhereClauseParam("param");
    }

    /**Sets <code>value</code> for bind variable param
     */
    public void setparam(String value) {
        setNamedWhereClauseParam("param", value);
    }

    /**executeQueryForCollection - overridden for custom java data source support.
     */
    protected void executeQueryForCollection(Object qc, Object params[], int noUserParams) 
    {
    super.executeQueryForCollection(qc, params, noUserParams);
    }

    /**hasNextForCollection - overridden for custom java data source support.
     */
    protected boolean hasNextForCollection(Object qc) {
        boolean bRet = super.hasNextForCollection(qc);
        return bRet;
    }

    /**createRowFromResultSet - overridden for custom java data source support.
     */
    protected ViewRowImpl createRowFromResultSet(Object qc, 
                                                 ResultSet resultSet) 
    {
        ViewRowImpl value = super.createRowFromResultSet(qc, resultSet);
        return value;
    }

    /**getEstimatedRowCount - overridden for custom java data source support.
     */
    public long getEstimatedRowCount() {
        long value = super.getEstimatedRowCount();
        return value;
    }
    
    public void consulta_anulados(String pUsuario,Number pNumPago,Date vFechaI,Date vFechaF){
        if (pNumPago!=null){
           this.setWhereClause("no_doc_fisico = " + pNumPago); 
        }else if (vFechaI !=null && vFechaF !=null){
          // this.setWhereClause("trunc(fecha_anulacion) between to_date('" + utils.getFechaFormato("dd/MM/yyyy",vFechaI)+ "','dd/MM/yyyy') and to_date('" + utils.getFechaFormato("dd/MM/yyyy",vFechaF) + "','dd/MM/yyyy') and creado_por='"+pUsuario+"'");             
             this.setWhereClause("trunc(fecha_anulacion) between to_date('" + utils.getFechaFormato("dd/MM/yyyy",vFechaI)+ "','dd/MM/yyyy') and to_date('" + utils.getFechaFormato("dd/MM/yyyy",vFechaF) + "','dd/MM/yyyy')");             
        }else{
          // this.setWhereClause("fecha_anulacion between sysdate-7 and sysdate+1 and creado_por='"+pUsuario+"'");  
             this.setWhereClause("fecha_anulacion between sysdate-7 and sysdate+1");  
        }
       this.setparam("1");
       this.setpUsuario(pUsuario);
       this.executeQuery(); 
        
    }

    /**Gets the bind variable value for pUsuario
     */
    public String getpUsuario() {
        return (String)getNamedWhereClauseParam("pUsuario");
    }

    /**Sets <code>value</code> for bind variable pUsuario
     */
    public void setpUsuario(String value) {
        setNamedWhereClauseParam("pUsuario", value);
    }
}
