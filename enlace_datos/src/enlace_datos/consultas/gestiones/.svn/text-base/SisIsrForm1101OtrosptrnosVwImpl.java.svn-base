package enlace_datos.consultas.gestiones;

import enlace_datos.consultas.gestiones.common.SisIsrForm1101OtrosptrnosVw;

import oracle.jbo.Row;
import oracle.jbo.domain.Number;
import oracle.jbo.server.ViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class SisIsrForm1101OtrosptrnosVwImpl extends ViewObjectImpl implements SisIsrForm1101OtrosptrnosVw {
    /**This is the default constructor (do not remove)
     */
    public SisIsrForm1101OtrosptrnosVwImpl() {
    }
    
    
    public void crearOtrosPatronos(){
        Row vFila;
        
        vFila=this.getCurrentRow();
        if (vFila!=null)
        vFila.setAttribute("Seleccionado",false);
        
        vFila=this.createRow();        
        vFila.setAttribute("Seleccionado",true);
        this.insertRow(vFila);
        
    }

    
    public void DisableAll(){
        Row[] vFilas;
        int vTam;
        vFilas=this.getAllRowsInRange();
        
        if (vFilas!=null)
        {
         vTam=vFilas.length;
         
             for (int i=0;i<vTam;i++)
             {
              
              vFilas[i].setAttribute("Seleccionado",false);
               
             }
        }
        
    }


    public void HabilitaActual(){
       Row vFila;
           vFila=this.getCurrentRow();
       if (vFila!=null)
       vFila.setAttribute("Seleccionado",true);
    }

    
    
    public Number getTotalRenta(){
        Row[] vFilas;
        int vTam;
        vFilas=this.getAllRowsInRange();
        Number vTotal=new Number(0);
        
     try{
        if (vFilas!=null)
        {
         vTam=vFilas.length;
         
             for (int i=0;i<vTam;i++)
             {              
              vTotal=vTotal.add((Number)vFilas[i].getAttribute("RentaNeta"));             
             }
        }
       
     }catch(Exception exep){
         
     }
     

     
     return vTotal;
    }  
    
}
