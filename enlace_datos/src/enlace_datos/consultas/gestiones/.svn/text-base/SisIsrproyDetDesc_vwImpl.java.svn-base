package enlace_datos.consultas.gestiones;

import enlace_datos.consultas.gestiones.common.SisIsrproyDetDesc_vw;

import oracle.jbo.Row;
import oracle.jbo.domain.Number;
import oracle.jbo.server.ViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class SisIsrproyDetDesc_vwImpl extends ViewObjectImpl implements SisIsrproyDetDesc_vw {
    /**This is the default constructor (do not remove)
     */
    public SisIsrproyDetDesc_vwImpl() {
    }
    
    //Función que busca un Ajuste en el View Object.
    public boolean BuscarAjuste(Number pIdAjuste){
        //System.out.println("El ajuste a buscar es el: " + pIdAjuste);
        boolean encontrado = false; //variable que indicará si se encontró
        Row[] vFilas;     
        vFilas=this.getAllRowsInRange();
        int vTamanio=vFilas.length;
        Number vIdAjuste;
        for (int i=0;i<vTamanio;i++){
            vIdAjuste = (Number)vFilas[i].getAttribute("IdAjuste");
            //System.out.println("El ajuste es: " + vIdAjuste);
            if ( vIdAjuste.equals(pIdAjuste) ) {
                //System.out.println("Ajuste Encontrado");
                encontrado = true;
                break;
            }
        }
        return encontrado;
    }
    
}
