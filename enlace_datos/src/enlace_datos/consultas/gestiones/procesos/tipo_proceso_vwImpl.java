package enlace_datos.consultas.gestiones.procesos;

import java.sql.ResultSet;

import oracle.jbo.server.ViewObjectImpl;
import oracle.jbo.server.ViewRowImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class tipo_proceso_vwImpl extends ViewObjectImpl {
    /**This is the default constructor (do not remove)
     */
    public tipo_proceso_vwImpl() {
    }

    /**Gets the bind variable value for nombres
     */
    public Object getnombres() {
        return (Object)getNamedWhereClauseParam("nombres");
    }

    /**Sets <code>value</code> for bind variable nombres
     */
    public void setnombres(Object value) {
        setNamedWhereClauseParam("nombres", value);
    }

    /**executeQueryForCollection - overridden for custom java data source support.
     */
    protected void executeQueryForCollection(Object qc, Object params[], int noUserParams) {
    
     if (this.getnombres()!=null){
         if (this.getnombres().equals("NUEVO")) {
        //  this.reset();
           this.setWhereClause("nombre not in ('POLIZA','PAGO','CARGA ARCHIVO','AUMENTO SALARIAL','COMPLEMENTO AUTOMATICO')");         
         }else if (this.getnombres().equals("LISTA")){
        //this.reset();
        this.setWhereClause("nombre not in ('POLIZA','CARGA ARCHIVO','AUMENTO SALARIAL')");         
         }else if (this.getnombres().equals("LISTA1")){
        //this.reset();
        this.setWhereClause("nombre not in ('POLIZA','CARGA ARCHIVO','REPOSICIONES','PAGO','AUMENTO SALARIAL')");         
         }else if (this.getnombres().equals("LISTAOPE")){
        this.setWhereClause("nombre in ('COMPLEMENTO')");     
         }
         else{
         this.reset();
         }
     }
     
    
    super.executeQueryForCollection(qc, params, noUserParams);
    }

    /**hasNextForCollection - overridden for custom java data source support.
     */
    protected boolean hasNextForCollection(Object qc) {
        boolean bRet = super.hasNextForCollection(qc);
        return bRet;
    }

    /**createRowFromResultSet - overridden for custom java data source support.
     */
    protected ViewRowImpl createRowFromResultSet(Object qc, 
                                                 ResultSet resultSet) {
        ViewRowImpl value = super.createRowFromResultSet(qc, resultSet);
        return value;
    }

    /**getEstimatedRowCount - overridden for custom java data source support.
     */
    public long getEstimatedRowCount() {
        long value = super.getEstimatedRowCount();
        return value;
    }
}
