package enlace_datos.consultas.gestiones.contrato;

import enlace_datos.consultas.gestiones.contrato.common.SisUnidadesEjecutoras_deps;

import java.util.Iterator;
import java.util.List;

import oracle.jbo.domain.Number;
import oracle.jbo.server.ViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class SisUnidadesEjecutoras_depsImpl extends ViewObjectImpl implements SisUnidadesEjecutoras_deps {
    /**This is the default constructor (do not remove)
     */
    public SisUnidadesEjecutoras_depsImpl() {
    }

    /**Gets the bind variable value for cond
     */
    public Number getcond() {
        return (Number)getNamedWhereClauseParam("cond");
    }

    /**Sets <code>value</code> for bind variable cond
     */
    public void setcond(Number value) {
        setNamedWhereClauseParam("cond", value);
    }
    
    public String getLista(Iterator vLista){
        String vTempo="";
        
        while (vLista.hasNext()){
          vTempo+=vLista.next().toString()+","; 
        }
            
          vTempo="(" + vTempo.substring(0,vTempo.length()-1) + ")";                
     return vTempo;   
    }
    
    public void consulta_por_Deps(List pDependencias){
    String vListaDeps;
      if (pDependencias!=null){
            vListaDeps=getLista(pDependencias.iterator());
            this.setWhereClause("id_dependencia in " +vListaDeps);
            this.setcond(new Number(1));
            this.executeQuery();    
      }else {
          this.setWhereClause("");
          this.setcond(new Number(0));
          this.executeQuery();    
          
      }
    
    }
    
}
