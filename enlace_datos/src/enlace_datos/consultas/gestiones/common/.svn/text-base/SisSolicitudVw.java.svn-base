package enlace_datos.consultas.gestiones.common;

import oracle.jbo.ViewObject;
import oracle.jbo.domain.Date;
import oracle.jbo.domain.Number;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---------------------------------------------------------------------
public interface SisSolicitudVw extends ViewObject {
    void crearSolicitud(String vTipo, Number vRegistroEmpleado, 
                        Number vIdContrato, String vCreadoPor);

    void actualizarEstado();

    void crearSolicitud2(String vTipo, Number vRegistroEmpleado, 
                         Number vIdContrato, String vCreadoPor, Number vAnio);

    void crearSolicitud3(String vTipo, Number vRegistroEmpleado, 
                         Number vIdContrato, Number pIdPartida, 
                         Number pIdPuesto, Number pNumeroPlaza, Number vAnio, 
                         String vCreadoPor, String vOrigen);

    void CambiarPlaza(Number vIdContrato, Number pIdPartida, Number pIdPuesto, 
                      Number pNumeroPlaza, Number vAnio, String pPartida, 
                      String pNomPuesto);

    void ConsultaPorId(Number pIdSolicitud);

    void reversar_gestion(Number pIdGestion, String pObserv, String pUsuario);

    void crearSolicitud4(String vTipo, Number vRegistroEmpleado,
                         Number vIdContrato, Number pIdPartida,
                         Number pIdPuesto, Number pNumeroPlaza, Number vAnio,
                         String vCreadoPor, String vOrigen,
                         String vTipoPuesto);

    String verificarRango_solicitud(Number id_contrato, String fecha_inicio, 
                                    String fecha_fin);


    String verificarTrabajadorFallecido(Number vRegistroEmpleado);


    String verificarChequeTramite_SolicitudFallecimiento(Number pIdSolicitud, 
                                                         Date fechaSolicitud);

    String verificarTrabajadorRetirado(Number vRegistroEmpleado);

    String obtenerIDSolicitudFallecimiento_autorizado(Number vRegistroEmpleado);

    String obtenerIDSolicitudRetiro_autorizado(Number vRegistroEmpleado);
}
