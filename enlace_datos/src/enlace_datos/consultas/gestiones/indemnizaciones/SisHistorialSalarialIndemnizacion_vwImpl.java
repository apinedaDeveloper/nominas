package enlace_datos.consultas.gestiones.indemnizaciones;


import enlace_datos.consultas.gestiones.indemnizaciones.common.SisHistorialSalarialIndemnizacion_vw;

import oracle.jbo.server.ViewObjectImpl;


// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class SisHistorialSalarialIndemnizacion_vwImpl extends ViewObjectImpl implements SisHistorialSalarialIndemnizacion_vw {
    /**This is the default constructor (do not remove)
     */
    public SisHistorialSalarialIndemnizacion_vwImpl() {
    }


    /**Gets the bind variable value for pRegistroEmpl
     */
    public String getpRegistroEmpl() {
        return (String)getNamedWhereClauseParam("pRegistroEmpl");
    }

    /**Sets <code>value</code> for bind variable pRegistroEmpl
     */
    public void setpRegistroEmpl(String value) {
        setNamedWhereClauseParam("pRegistroEmpl", value);
    }

    /*public String obtener_promedio_sueldos_post_mortem() {
        SisHistorialSalarialIndemnizacion_vwRowImpl salario;
        Object listadoSalarios[] = this.getAllRowsInRange();
        int longitud = listadoSalarios.length;
        double suma = 0, promedio = 0;
        int contador = 0;
        while (contador < longitud) {
            salario = 
                    (SisHistorialSalarialIndemnizacion_vwRowImpl)listadoSalarios[contador];
            suma += Double.valueOf(salario.getAttribute("Nominal").toString());
            contador++;
            if (contador == 6 || contador == longitud) {
                promedio = suma / contador;
                break;
            }
        } // fin while
        return String.valueOf(promedio);
    }*/

    //Obtiene el total de sueldos devengandos durante los 
    //últimos doce meses de la relación laboral

    public String obtener_total_sueldos() {
        SisHistorialSalarialIndemnizacion_vwRowImpl fila;
        Object listado[] = this.getAllRowsInRange();
        int longitudListado = listado.length;
        double suma = 0;
        int cont = 0;
        while (cont < longitudListado && cont < 12) {
            fila = (SisHistorialSalarialIndemnizacion_vwRowImpl)listado[cont];
            suma += Double.valueOf(fila.getAttribute("Nominal").toString());
            cont++;
        } // fin while
        return String.valueOf(suma);
    }

    //Obtiene el número de sueldos a tomar en cuenta para el promedio de sueldos

    /*public String obtener_numero_sueldos() {
        Object listado[] = this.getAllRowsInRange();
        int longitudListado = listado.length;
        if (longitudListado > 12) {
            longitudListado = 12;
        }
        return String.valueOf(longitudListado);
    }*/
}
