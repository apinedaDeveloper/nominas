package enlace_datos.consultas.gestiones.anexo;


import enlace_datos.consultas.gestiones.anexo.common.sis_movimiento_plaza;

import oracle.jbo.domain.Date;
import java.sql.ResultSet;

import java.util.ArrayList;
import oracle.jbo.domain.Number;
import oracle.jbo.NameValuePairs;
import oracle.jbo.server.ViewObjectImpl;
import oracle.jbo.server.ViewRowImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class sis_movimiento_plazaImpl extends ViewObjectImpl implements sis_movimiento_plaza {
    /**This is the default constructor (do not remove)
     */
    public sis_movimiento_plazaImpl() {
        
    }
    //metodo utilizado para crear el detalle de las promociones docentes y reclasificaciones
    public void crear_promocion(Number vPartida, Number vPuesto, Number vPlaza, Number vCorrelativo, Number vHoras, Date vFechIni,
    Date vFechFin, Number vSueldo, Number vEscalaf, Number vComplemen, String vPeriodoPlaza, Number vPuestoNuevo, Number vSueldoNuevo, 
    Number vEscalafNuevo,Number vIdPromocion,Number vRegistroEmpleado){
   // System.out.println("si llegue al metodo");
       /* if (valParametros.equals(null)){
            System.out.println("viene nulo el arraylist gui gui gui");
        }
        else {
        */
            sis_movimiento_plazaRowImpl  filaDetalle;
           // NameValuePairs valDetalle;
            //valDetalle = (NameValuePairs) valParametros.get(0);
            NameValuePairs datos;
            datos = new NameValuePairs();
            datos.setAttribute("Operacion","B");
             datos.setAttribute("IdPartida",vPartida);
             datos.setAttribute("IdPuesto",vPuesto);
             datos.setAttribute("NumeroPlaza",vPlaza);
             datos.setAttribute("Correlativo",vCorrelativo);
             datos.setAttribute("CantidadHoras",vHoras);
             datos.setAttribute("FechaInicio",vFechIni);
             datos.setAttribute("FechaFin",vFechFin);
             datos.setAttribute("SueldoBase",vSueldo);
             datos.setAttribute("Escalafon",vEscalaf);
             datos.setAttribute("ComplSalarial",vComplemen);
             datos.setAttribute("PeriodoPlaza",vPeriodoPlaza);
            datos.setAttribute("idPromocion",vIdPromocion);
            datos.setAttribute("RegistroEmpleado",vRegistroEmpleado);
            filaDetalle = (sis_movimiento_plazaRowImpl) this.createAndInitRow(datos);
            this.insertRow(filaDetalle);
            vIdPromocion = vIdPromocion.add(5);
            //calculo el sueldo  para la nueva plaza, segun esacala y No. de horas.
            vSueldoNuevo = vSueldoNuevo.multiply(vHoras);
            //actualizando valores para crear la plaza nueva.
            datos.setAttribute("Operacion","A");
            datos.setAttribute("IdPuesto",vPuestoNuevo);
            datos.setAttribute("Escalafon",vEscalafNuevo);
            datos.setAttribute("SueldoBase",vSueldoNuevo);
            datos.setAttribute("idPromocion",vIdPromocion);
            //creando la nueva fila en el viewobject
            filaDetalle = (sis_movimiento_plazaRowImpl) this.createAndInitRow(datos);
            this.insertRow(filaDetalle);
        //}
        
        //System.out.println("termine el metodo crear anexo promocion");
    }

    /**executeQueryForCollection - overridden for custom java data source support.
     */
    protected void executeQueryForCollection(Object qc, Object params[], int noUserParams) {super.executeQueryForCollection(qc, params, noUserParams);
    }

    /**hasNextForCollection - overridden for custom java data source support.
     */
    protected boolean hasNextForCollection(Object qc) {
        boolean bRet = super.hasNextForCollection(qc);
        return bRet;
    }

    /**createRowFromResultSet - overridden for custom java data source support.
     */
    protected ViewRowImpl createRowFromResultSet(Object qc, 
                                                 ResultSet resultSet) {
        ViewRowImpl value = super.createRowFromResultSet(qc, resultSet);
        return value;
    }

    /**getEstimatedRowCount - overridden for custom java data source support.
     */
    public long getEstimatedRowCount() {
        long value = super.getEstimatedRowCount();
        return value;
    }
}
