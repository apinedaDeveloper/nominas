package enlace_datos.consultas.gestiones.empleado;

import enlace_datos.consultas.gestiones.empleado.common.Sis_foto_empleado_vw;

import enlace_datos.util.utils_DB;
import oracle.jbo.domain.Number;
import oracle.jbo.server.ViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class Sis_foto_empleado_vwImpl extends ViewObjectImpl implements Sis_foto_empleado_vw {
    /**This is the default constructor (do not remove)
     */
    public Sis_foto_empleado_vwImpl() {
    }

    /**Gets the bind variable value for pRegistroEmpleado
     */
    public Number getpRegistroEmpleado() {
        return (Number)getNamedWhereClauseParam("pRegistroEmpleado");
    }

    /**Sets <code>value</code> for bind variable pRegistroEmpleado
     */
    public void setpRegistroEmpleado(Number value) {
        setNamedWhereClauseParam("pRegistroEmpleado", value);
    }
    
    //Procedimiento que valida la foto del empleado
    public void validarFotoEmpleado(Number pIdFoto){
        String query =
        "update sis_archivo_adjunto set validado = 1 where id_archivo_adjunto = " + pIdFoto;
        utils_DB.getEjecutarConsulta(this.getDBTransaction(), query);
        this.getDBTransaction().commit();
    }
    
    //Procedimiento que retorna la foto confirmada del empleado
    public void consultarConfirmadas(Number pRegEmpleado){
        String str_query="";
        if (pRegEmpleado!=null){
            this.setpRegistroEmpleado(pRegEmpleado);
            str_query = "validado = 1"; 
            this.setWhereClause(str_query);
            this.executeQuery(); 
        }
    }
    
    //Procedimiento que revierte la validación de una valida la foto del empleado
    public void revertirValidacionFoto(Number pIdFoto){
        String query =
        "update sis_archivo_adjunto set validado = null where id_archivo_adjunto = " + pIdFoto;
        utils_DB.getEjecutarConsulta(this.getDBTransaction(), query);
        this.getDBTransaction().commit();
    }
}
