package enlace_datos.consultas.gestiones.empleado_ficha;


import enlace_datos.consultas.gestiones.empleado_ficha.common.Sis_documento_adjunto_vw;

import enlace_datos.util.utils_DB;

import oracle.jbo.domain.Number;
import oracle.jbo.server.ViewObjectImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class Sis_documento_adjunto_vwImpl extends ViewObjectImpl implements Sis_documento_adjunto_vw {
    /**This is the default constructor (do not remove)
     */
    public Sis_documento_adjunto_vwImpl() {
    }

    /**Gets the bind variable value for pRegistroEmpleado
     */
    public Number getpRegistroEmpleado() {
        return (Number)getNamedWhereClauseParam("pRegistroEmpleado");
    }

    /**Sets <code>value</code> for bind variable pRegistroEmpleado
     */
    public void setpRegistroEmpleado(Number value) {
        setNamedWhereClauseParam("pRegistroEmpleado", value);
    }
    
    //Procedimiento que confirma un documento adjunto
    public void validarDocumentoAdjunto(Number pIdArchivoAdjunto){
        String query =
        "update sis_archivo_adjunto set validado = 1 where id_archivo_adjunto = " + pIdArchivoAdjunto;
        utils_DB.getEjecutarConsulta(this.getDBTransaction(), query);
        this.getDBTransaction().commit();
    }


    public void consultarConfirmados(Number pRegistroEmpleado, Number pTipoInicial){
        String query;
        if ( pRegistroEmpleado != null && pTipoInicial != null) {
            query = "validado = 1";
            this.setpRegistroEmpleado(pRegistroEmpleado);
            this.setpTipoInicial(pTipoInicial);
            this.setWhereClause(query);
            this.executeQuery();
        }
    }
    
    //Procedimiento que revierte la validación(confirmación) de un documento ya confirmado
    public void revertirValidacionDocumento(Number pIdDocumento){
        String query =
        "update sis_archivo_adjunto set validado = null where id_archivo_adjunto = " + pIdDocumento;
        utils_DB.getEjecutarConsulta(this.getDBTransaction(), query);
        this.getDBTransaction().commit();
    }

    /**Gets the bind variable value for pTipoInicial
     */
    public Number getpTipoInicial() {
        return (Number)getNamedWhereClauseParam("pTipoInicial");
    }

    /**Sets <code>value</code> for bind variable pTipoInicial
     */
    public void setpTipoInicial(Number value) {
        setNamedWhereClauseParam("pTipoInicial", value);
    }
}
